/* any example

commenting out bc using same variable names in unknown.ts which can cause conflict errors
can be eliminated using namespaces will get into later

let val: any = 22;
val = 'string value';
val = new Array();
val.push(33);

console.log(val);

*/
